DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=consignado

SECRET_KEY=change-me
JWT_SECRET_KEY=change-me-too

# Pode ser um dominio unico ou varios separados por virgula
# Exemplo:
# CORS_ORIGINS=https://seu-frontend.up.railway.app,https://www.seudominio.com
CORS_ORIGINS=http://localhost:5173

# No Railway, se montar volume em /app/storage, use este valor.
STORAGE_ROOT=/app/storage
